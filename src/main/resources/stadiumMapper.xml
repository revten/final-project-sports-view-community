<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="StadiumMapper">
	<select id="getLogoBySports" resultType="ClubImageDTO">
		select ci.*, c.league_id from tb_club_image ci, tb_club c
		where ci.club_id = c.id and c.league_id = #{league_id} and sort = 0
	</select>
</mapper>